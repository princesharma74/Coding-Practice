#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    int T; 
    cin >> T; 
    while(T--){
        int a, b; 
        cin >> a >> b; 
        cout << (b - a) << endl; 
    }
    return 0;
}