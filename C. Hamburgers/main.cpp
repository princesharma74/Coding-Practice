#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    string str; 
    cin >> str; 
    int nb, ns, nc; 
    cin >> nb >> ns >> nc; 
    int pb, ps, pc; 
    cin >> pb >> ps >> pc; 
    ll r; 
    cin >> r; 
    
    return 0;
}