#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    int t; 
    cin >> t; 
    while(t--){
        int n, m; 
        cin >> n >> m; 
        vector<int> a(n), b(m);
        for(int &it : a) cin >> it; 
        for(int &it : b) cin >> it;
    }
    return 0;
}